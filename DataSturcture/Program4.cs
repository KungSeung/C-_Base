using System;
using System.Collections.Generic;
using System.Text;

namespace DataSturcture
{
	class Program4
	{
		/*static void Main(string[] args)
		{
			// List - 동적배열, Index의 변화가 자주 일어나서 그렇게 효율적이진 않다
			List<int> list = new List<int>();
			for (int i = 0; i < 5; i++)
				list.Add(i);

			// 삽입, 삭제
			list.Insert(2, 999);	// Index2에 999에 넣음
			bool success = list.Remove(3);  // 처음 만난 3만 지움 - bool 값
			list.RemoveAt(0);       // Index 값에 해당하는 값 지움
			list.Clear();		// 전체 삭제

			for (int i = 0; i < 5; i++)
				Console.WriteLine(list[i]);
			foreach(int num in list)
				Console.WriteLine(num);
		}*/
	}
}
